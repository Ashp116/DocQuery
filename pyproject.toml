[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["encoder", "model", "ocr", "train", "train_dataset", "main"]

[project]
name = "doc-query"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.13.0",
    "bitsandbytes>=0.49.2",
    "datasets>=4.8.5",
    "easyocr>=1.7.2",
    "einops>=0.8.2",
    "evaluate>=0.4.6",
    "huggingface-hub>=0.36.2",
    "opencv-python>=4.13.0.92",
    "peft>=0.19.1",
    "pillow>=12.2.0",
    "qwen-vl-utils>=0.0.14",
    "tokenizers>=0.20.3",
    "tqdm>=4.67.3",
    "transformers==4.45.0",
    "trl>=0.12.2",
]
